.DS_Store
*.log
db_config.local.php
tests/sandbox/
tests/out/

# データ更新パイプラインの作業ディレクトリ
beer-data-work/

# 参考用のスクリーンショット(他社サイトの画面・商品画像を含むため公開もcommitもしない)
ref/

# 評価用の写真(個人の写真・約26MB)。正解データ expected.json だけを追跡する
data/sample/*.jpeg
data/sample/*.jpg
data/sample/*.png

# subagent-driven-development の作業領域(台帳・ブリーフ・レビュー資材)
.superpowers/

# Anthropic の APIキー。db_config.local.php と同じ方式で、実値はリポジトリに置かない
api_config.local.php

# アップロードされた写真(利用者の個人写真)。公開面に出さないのと同じ理由でcommitもしない(設計書 §9)
img/upload/*
!img/upload/.htaccess

# tests/eval/run_eval.php の実行結果(実行日ごとに生成される)。
# 素の応答まで含む生成物で、--replay の動作確認でも同名パターンで作られるため追跡しない。
# 比較用のサマリは docs/data-log/ に人が残す
tests/eval/result-*.json
